Drop generated media files into this folder.

Expected files:
- `hero-loop.mp4` or `hero-loop.webm` for the main cinematic video slot
- `hero-poster.svg` or a real poster frame image to preview the hero scene
- future section images such as `mobility.jpg`, `energy.jpg`, `public-space.jpg`

The site is already structured so these files can be swapped in without changing the layout.