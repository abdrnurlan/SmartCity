Drop generated media files into this folder.

Expected files:
- `hero-loop.mp4` or `hero-loop.webm` for the main cinematic video slot
- `hero-poster.svg` remains the default placeholder poster for now
- future section images such as `mobility.jpg`, `energy.jpg`, `public-space.jpg`

Recommended replacement flow:
1. Generate the hero still in ChatGPT Imagen.
2. Animate that still in Kling UI with image-to-video.
3. Export the final clip as `hero-loop.mp4` and place it in this folder.
4. Keep `hero-poster.svg` until you decide to do a small follow-up pass for a final raster poster format.